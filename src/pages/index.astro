---
const lang =
  Astro.cookies.get("lang")?.value ??
  ((Astro.request.headers.get("accept-language") || "")
    .toLowerCase()
    .startsWith("en")
    ? "en"
    : "es");
return Astro.redirect("/" + lang);
---
