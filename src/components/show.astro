---
interface Props {
  when: unknown;
}

const { when } = Astro.props;

// Handle edge cases more robustly
const shouldRender =
  Boolean(when) && !(Array.isArray(when) && when.length === 0);
---

{shouldRender && <slot />}
